// IntegrationCredential entity - matches original Base44 structure
import { createEntityWrapper } from '@/api/entities';

export const IntegrationCredential = createEntityWrapper('IntegrationCredential');
